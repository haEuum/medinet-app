import { View, Text } from "react-native";

const Chat = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>여긴 Chat 탭입니다</Text>
        </View>
    );
};

export default Chat;