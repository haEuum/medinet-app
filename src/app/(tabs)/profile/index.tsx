import { View, Text } from "react-native";

const Profile = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>여긴 Profile 탭입니다</Text>
        </View>
    );
};

export default Profile;